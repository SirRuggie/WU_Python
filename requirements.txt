# Schedules jobs like timers or repeated tasks (e.g. cron jobs)
APScheduler

# Main Discord API wrapper for bots.
# PINNED DELIBERATELY. These two move together: hikari-lightbulb 3.0.3 declares
# `hikari~=2.3.1` (>=2.3.1,<2.4.0), so 2.3.5 is the highest hikari it accepts -
# which is why the bot logs "consider upgrading to 2.5.0" on every boot and why
# the 2.4.1 upgrade in 397e3ba did not survive: leaving these unpinned let a
# later `pip install -r requirements.txt` re-resolve straight back down to 2.3.5.
# Upgrading is one coupled move (hikari 2.5.0 + hikari-lightbulb 3.2.5), never
# one package alone. See docs/hikari-lightbulb-versions.md.
hikari==2.3.5

# Command framework for hikari (handles slash and prefix commands)
hikari-lightbulb==3.0.3

# Loads .env file contents into environment variables
python-dotenv

# MongoDB driver (synchronous)
pymongo

# Handles timezones and datetime formatting (used by APScheduler)
pendulum

# Clash of Clans API wrapper (community-maintained).
# PINNED AT THE DEPLOYED VERSION. 3.9.1 is what the box runs and what every
# Clash code path here has been exercised against.
#
# 3.10.0 exists and is worth taking LATER, deliberately, on its own deploy:
#   3.9.2  datetime.utcnow() -> datetime.now() (kills 3.12 DeprecationWarnings)
#   3.9.2  handles the CoC API returning a TEXT response instead of JSON -
#          directly relevant, since every Clash call goes through a third-party
#          proxy that can return an HTML error page during an outage
#   3.10.0 TH18 game data (Totem Spell, Meteor Golem)
#
# Not taken now because the TH18 half is inert here - main.py sets
# load_game_data=LoadGameData(default=False), so troop/spell tables are never
# loaded - and the useful half is a change to the ERROR path, which deserves a
# deliberate deploy rather than riding along with a pin whose whole purpose is
# to reproduce the known-good state. See docs/hikari-lightbulb-versions.md.
coc.py==3.9.1

# Image processing library (resize, convert, etc.)
Pillow

# HTTP library for sending requests (e.g. API calls)
requests

# Async version of requests (works with async code)
aiohttp

# Uploads and manages media on Cloudinary
cloudinary

# Async Reddit API wrapper (used for bots interacting with Reddit)
asyncpraw

# HTML parser for the FWA points scraper
beautifulsoup4

# iCalendar parser for the BAND FWA sync feeds (band_sync_ical task).
# Resolves DTSTART;TZID=Asia/Hong_Kong against a real timezone database rather than
# reading the digits, which is what keeps sync alerts from drifting an hour across a
# DST boundary. Needs system tzdata on slim containers: if ZoneInfoNotFoundError shows
# up in the logs, `pip install tzdata`.
icalendar
